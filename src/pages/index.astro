---
import { Debug } from 'astro:components';
import Layout from '../layouts/Layout.astro';
const ListOeuvre = await Astro.locals.pb.collection('Oeuvre').getFullList();

---

<Layout pageTitle="Accueil">
	<Debug {ListOeuvre}/>
</Layout>
